<h1>Listing work_orders</h1>

<table>
  <tr>
    <th>Author</th>
    <th>Owner</th>
    <th>Client</th>
    <th>Payment type</th>
    <th>Memo</th>
    <th>Created at</th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @work_orders.each do |work_order| %>
  <tr>
    <td><%= work_order.author_id %></td>
    <td><%= work_order.owner_id %></td>
    <td><%= work_order.client_id %></td>
    <td><%= work_order.payment_type_id %></td>
    <td><%= work_order.memo %></td>
    <td><%= link_to 'Show', work_order %></td>
    <td><%= link_to 'Edit', edit_work_order_path(work_order) %></td>
    <td><%= link_to 'Destroy', work_order, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Work order', new_work_order_path %>
